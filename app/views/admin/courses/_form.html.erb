<%= form_for(@admin_course) do |f| %>
  <% if @admin_course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_course.errors.count, "error") %> prohibited this admin_course from being saved:</h2>

      <ul>
      <% @admin_course.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="clearfix">
    <%= f.label :title %>
  <div class="input"><%= f.text_field :title %></div>
  
  <div class="clearfix">
    <%= f.label :number %>
  <div class="input"><%= f.text_field :number %></div>
  
  <div class="clearfix">
    <%= f.label :school_id %>
  <div class="input"><%= f.text_field :school_id %></div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
