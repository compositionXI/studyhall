<%= form_for(@admin_instructor) do |f| %>
  <% if @admin_instructor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_instructor.errors.count, "error") %> prohibited this admin_instructor from being saved:</h2>

      <ul>
      <% @admin_instructor.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
