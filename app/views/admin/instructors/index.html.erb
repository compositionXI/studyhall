<h1>Listing admin_instructors</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @admin_instructors.each do |admin_instructor| %>
  <tr>
    <td><%= link_to 'Show', admin_instructor %></td>
    <td><%= link_to 'Edit', edit_admin_instructor_path(admin_instructor) %></td>
    <td><%= link_to 'Destroy', admin_instructor, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Instructor', new_admin_instructor_path %>
