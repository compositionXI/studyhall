<%= form_for [:admin, @course_offering_import], :html => {:class => "form-stacked"} do |f| %>
  <% if @course_offering_import.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@course_offering_import.errors.count, "error") %> prohibited this course_offering_import from being saved:</h2>
      
      <ul>
      <% @course_offering_import.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="clearfix">
    <%= f.label :school %>
  <div class="input"><%= select "course_offering_import", "school_id", School.all.collect {|s| [s.name, s.id]} %></div>
  </div>
  <div class="clearfix">
    <%= f.label :term %>
    <div class="input"><%= select "course_offering_import", "term_id", Term.all.collect {|t| [t.name, t.id]} %></div>
  </div>
  <div class="clearfix">
    <%= f.label :file %>
    <div class="input"><%= f.file_field :course_offering_import %></div>
  </div>
  <div>
    <%= f.submit "Submit", :class => "btn primary" %>
    <%= link_to 'Back', admin_static_pages_path, :class => "btn" %>
  </div>
<% end %>