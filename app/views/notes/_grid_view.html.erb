<div class="grid_view">
  <div class="preview">
    <div class="fields">
      <a class="name" href="<%= note_path(note) %>">
        <span><%= note.name %></span>
      </a>
    </div>

    <% unless note.shareable? %>
      <a href="#" class="lock_icon icon">Share</a>
    <% end %>
    <% if @edit_all %>
      <span class="drag_handle icon">+</span>
    <% end %>
  </div>
</div>
