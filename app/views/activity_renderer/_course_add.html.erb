<li class="activity_message">
  <%= link_to image_tag(user.photo_url(:medium)), user, title: user.name %>
  <p><%= link_to user.name, user %> added a course: <%= link_to offering.course.title, class_path(offering) %></p>
</li>
