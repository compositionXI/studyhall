<li class="activity_message">
  <%= link_to image_tag(user.photo_url(:medium)), user, title: user.title_name %>
  <p><%= link_to user.title_name, user %> just shared a <%= object.class.to_s.downcase %>: <%= link_to truncate(object.name, length: 20), object%> with you</p>
</li>
