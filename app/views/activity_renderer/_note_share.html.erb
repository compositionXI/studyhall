<li class="activity_message">
  <%= link_to image_tag(user.photo_url(:medium)), user, title: user.name %>
  <p><%= link_to user.name, user %> just shared a <%= note.class.to_s.downcase %>: <%= link_to note.name, note %></p>
</li>
